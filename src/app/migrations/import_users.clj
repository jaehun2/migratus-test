(ns app.migrations.import-users)

(defn migrate-up [config]
  ;; do stuff here
  (println "import-user code based migrate-up")
  )

(defn migrate-down [config]
  ;; maybe undo stuff here
  (println "import-user code based migrate-down")
  )